const name = "Hemant"
const repocount = 50
//console.log(name + repocount + "Value");
console.log(`Hello my name is ${name} and my repo count is ${repocount}`);
// `-> isko kahte hai backtick:


const gameName = new String('Hemant')
console.log(gameName.length);
console.log(gameName.toUpperCase);
console.log(gameName.charAt(2));
console.log(gameName.indexOf('t'));
const newString = gameName.substring(0, 4)
console.log(newString);
const anotherString = gameName.slice(-8,4)
console.log(anotherString);
/*
| Method           | Purpose                               |
| ---------------- | ------------------------------------- |
| `.length`        | Get length                            |
| `.charAt()`      | Character at index                    |
| `.at()`          | Character at index, supports negative |
| `.toUpperCase()` | Uppercase                             |
| `.toLowerCase()` | Lowercase                             |
| `.trim()`        | Remove outer spaces                   |
| `.trimStart()`   | Remove starting spaces                |
| `.trimEnd()`     | Remove ending spaces                  |
| `.includes()`    | Check existence                       |
| `.startsWith()`  | Check beginning                       |
| `.endsWith()`    | Check ending                          |
| `.indexOf()`     | Find first index                      |
| `.lastIndexOf()` | Find last index                       |
| `.search()`      | Search pattern                        |
| `.replace()`     | Replace first match                   |
| `.replaceAll()`  | Replace all matches                   |
| `.slice()`       | Extract part                          |
| `.substring()`   | Extract part                          |
| `.split()`       | String → Array                        |
| `.concat()`      | Join strings                          |
| `.repeat()`      | Repeat string                         |
| `.padStart()`    | Add padding at start                  |
| `.padEnd()`      | Add padding at end                    |

*/





